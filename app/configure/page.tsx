import { PrototypeHandoff } from "../_components/prototype-handoff";
export default function Configure() { return <PrototypeHandoff title="Select Porsche to Configure" body="Please select a model line before configuring. Context from the homepage is preserved in the URL." />; }
