import { PrototypeHandoff } from "../_components/prototype-handoff";
export default function Models() { return <PrototypeHandoff title="Explore Model Lines" body="You are at the model discovery entry point. Models selected from the homepage continue here." />; }
