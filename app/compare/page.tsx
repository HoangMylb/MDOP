import { PrototypeHandoff } from "../_components/prototype-handoff";
export default function Compare() { return <PrototypeHandoff title="Compare Models" body="Selected vehicle models are passed via URL and stored locally so you can continue your evaluation." />; }
